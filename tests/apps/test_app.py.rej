diff a/tests/apps/test_app.py b/tests/apps/test_app.py	(rejected hunks)
@@ -1,5 +1,5 @@
 def test_importing_app():
-    # this will raise an exception if pydantic model validation fails for th app
+    # this will raise an exception if pydantic model validation fails for the app
     from nomad_lap_schema.apps import app_entry_point
 
     assert app_entry_point.app.label == 'NewApp'
